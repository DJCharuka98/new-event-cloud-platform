apiVersion: v1
kind: Service
metadata:
  name: event-service-blue-preview
spec:
  type: ClusterIP
  selector:
    app: event-service
    color: blue
  ports:
    - port: 8000
      targetPort: 8000

---
apiVersion: v1
kind: Service
metadata:
  name: event-service-green-preview
spec:
  type: ClusterIP
  selector:
    app: event-service
    color: green
  ports:
    - port: 8000
      targetPort: 8000
