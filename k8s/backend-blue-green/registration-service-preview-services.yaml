apiVersion: v1
kind: Service
metadata:
  name: registration-service-blue-preview
spec:
  type: ClusterIP
  selector:
    app: registration-service
    color: blue
  ports:
    - port: 8000
      targetPort: 8000

---
apiVersion: v1
kind: Service
metadata:
  name: registration-service-green-preview
spec:
  type: ClusterIP
  selector:
    app: registration-service
    color: green
  ports:
    - port: 8000
      targetPort: 8000
