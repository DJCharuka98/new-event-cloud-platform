apiVersion: v1
kind: Service
metadata:
  name: new-event-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: new-event-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
